================================================================================
CREATE MODEL with OPTIONS
================================================================================

CREATE MODEL my_model OPTIONS (model_type = 'LINEAR_REG') AS SELECT * FROM training;

--------------------------------------------------------------------------------

(program
  (statement
    (bq_create_model
      (keyword_create)
      (keyword_model)
      (object_reference
        name: (identifier))
      (bq_options_clause
        (keyword_options)
        key: (identifier)
        value: (literal))
      (keyword_as)
      (create_query
        (select
          (keyword_select)
          (select_expression
            (term
              value: (all_fields))))
        (from
          (keyword_from)
          (relation
            (object_reference
              name: (identifier))))))))

================================================================================
ML.PREDICT in FROM clause
================================================================================

SELECT * FROM ML.PREDICT(MODEL my_model, TABLE input);

--------------------------------------------------------------------------------

(program
  (statement
    (select
      (keyword_select)
      (select_expression
        (term
          value: (all_fields))))
    (from
      (keyword_from)
      (relation
        (invocation
          (object_reference
            schema: (identifier)
            name: (identifier))
          parameter: (term
            value: (field
              name: (identifier))
            alias: (identifier))
          parameter: (term
            value: (field
              name: (identifier))
            alias: (identifier)))))))

================================================================================
ML.EVALUATE in FROM clause
================================================================================

SELECT * FROM ML.EVALUATE(MODEL my_model, TABLE test_data);

--------------------------------------------------------------------------------

(program
  (statement
    (select
      (keyword_select)
      (select_expression
        (term
          value: (all_fields))))
    (from
      (keyword_from)
      (relation
        (invocation
          (object_reference
            schema: (identifier)
            name: (identifier))
          parameter: (term
            value: (field
              name: (identifier))
            alias: (identifier))
          parameter: (term
            value: (field
              name: (identifier))
            alias: (identifier)))))))
